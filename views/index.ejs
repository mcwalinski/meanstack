<!DOCTYPE html>
<html ng-app="notesApp"><html>
  <% include partials/nav.ejs %>  
  <body ng-controller="notesCtrl">
    <div class="container-fluid" ng-init="getNotes()">
      <div class="row">
        <div class="col-sm-11 col-sm-offset-1 col-md-12 col-md-offset-0 main">
          
          <h1 class="page-header">Dashboard - Notes</h1>
          
          <div class="col-sm-12">
            Hello welcome to your notes.  You can search below.
          </div>
          <br><br>
          <form class="col-md-12 form-group input-group-lg">
            <input ng-model="query" type="text" class="form-control" placeholder="Search..." autofocus>
          </form>

          <div class="table-responsive">
            <table class="table table-striped">
              <thead>
                <tr>
                  <th>
                    <a href="#" ng-click="sortType = 'title'; sortReverse = !sortReverse">
                      Name
                      <span ng-show="sortType == 'title' && !sortReverse" class="fa fa-caret-up"></span>
                      <span ng-show="sortType == 'title' && sortReverse" class="fa fa-caret-down"></span>
                    </a>
                  </th>
                  <th>
                    Email Address
                  </th>
                  <th>
                    <a href="#" ng-click="sortType = 'title'; sortReverse = !sortReverse">
                      First Choice
                      <span ng-show="sortType == 'title' && !sortReverse" class="fa fa-caret-up"></span>
                      <span ng-show="sortType == 'title' && sortReverse" class="fa fa-caret-down"></span>
                    </a>
                  </th>
                  <th>
                    <a href="#" ng-click="sortType = 'title'; sortReverse = !sortReverse">
                      Second Choice
                      <span ng-show="sortType == 'title' && !sortReverse" class="fa fa-caret-up"></span>
                      <span ng-show="sortType == 'title' && sortReverse" class="fa fa-caret-down"></span>
                    </a>
                  </th>
                  <th>
                    <a href="#" ng-click="sortType = 'title'; sortReverse = !sortReverse">
                      Third Choice
                      <span ng-show="sortType == 'title' && !sortReverse" class="fa fa-caret-up"></span>
                      <span ng-show="sortType == 'title' && sortReverse" class="fa fa-caret-down"></span>
                    </a>
                  </th>
                </tr>
              </thead>
              <tbody>
                <tr ng-repeat="note in userNotes | filter:query | unique: '_id' | orderBy:sortType:sortReverse">
                  <td><a href="/note/{{note._id}}">{{ note.first_and_last_name_1 }}</a></td>
                  <td><a href="/note/{{note._id}}">{{ note.email_address_2 }}</a></td>
                  <td><a href="/note/{{note._id}}">{{ note.first_choice_4 }}</a></td>
                  <td><a href="/note/{{note._id}}">{{ note.second_choice_5 }}</a></td>
                  <td><a href="/note/{{note._id}}">{{ note.third_choice_17 }}</a></td>
                </tr>
              </tbody>
            </table>
          </div>
        </div>
      </div>
    </div>

   </body>
   <% include partials/javascripts.ejs %>  
</html>